import CustomContainer from "@/components/ui/CustomContainer";

const Dashboard = () => {
  return (
    <section className="bg-component-base-2 min-h-screen">
    </section>
  );
};

export default Dashboard;
